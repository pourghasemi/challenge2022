export default {
  setTabs(state, tabs) {
    state.tabs = tabs;
  },
  setTabdata(state, tabData) {
    state.tabdata = tabData;
  },
  setPlugings(state, plugins) {
    state.plugins = plugins;
  },
};
